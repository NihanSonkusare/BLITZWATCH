fastapi>=0.111
uvicorn[standard]>=0.29
gunicorn>=22.0
pymupdf>=1.24          # PDF parsing — use this, NOT PyPDF2
httpx>=0.27
pydantic>=2.7
celery>=5.4
redis>=5.0
supabase>=2.5
pytesseract>=0.3       # OCR fallback
Pillow>=10.3           # Image validation for headshots
python-multipart>=0.0.9  # Multipart file upload support
sentry-sdk[fastapi]>=2.0
anthropic>=0.29        # Claude API
openai>=1.0            # GPT-4o fallback
python-dotenv>=1.0
ruff>=0.4              # Linting + formatting
pytest>=8.0            # Testing
pytest-asyncio>=0.23   # Async test support
httpx>=0.27            # Also used in tests
