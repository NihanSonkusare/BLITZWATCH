"""
Placeholder for the AI generation pipeline Celery tasks.
Steps: Extract → Script → Review Gate → Video → Publish
"""
# from app.core.celery_app import celery_app
# from app.core.supabase_client import get_supabase
#
# @celery_app.task(bind=True, max_retries=3)
# def run_pipeline(self, job_id: str, module_id: str):
#     """Main pipeline task — to be implemented in Phase 6."""
#     pass
